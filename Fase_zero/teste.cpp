#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef unsigned long long ull;

int to_A(ll a, ll b, ll c){
    ll sum=0;
}

int to_B(ll a, ll b, ll c){
    ll sum=0;
   
}

int to_C(ll a, ll b, ll c){
    ll sum=0;
   
}
int main()
{
    ull a, b, c;
    cin >> a >> b >> c;
    char carac; cin >> carac;


    if(carac == 'A')
        cout<<to_A(a,b,c)<<endl;
    else if(carac == 'B')
        cout<<to_B(a,b,c)<<endl;
    else
        cout<<to_C(a,b,c)<<endl;

    return 0;
}


