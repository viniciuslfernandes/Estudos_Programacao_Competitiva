#include <bits/stdc++.h>
using namespace std;
int main(){

    int n, h;
    double s;
    cin>>n>>h>>s;
    cout.precision(2);
    cout.setf(ios::fixed);
    cout<<"NUMBER = "<<n<<endl<<"SALARY = U$ "<<h*s<<endl;
    return 0;
}